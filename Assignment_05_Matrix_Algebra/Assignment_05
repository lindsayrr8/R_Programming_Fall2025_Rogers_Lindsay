
### Relevant blog post: https://rlanguagejournal.blogspot.com/2025/09/module-5-matrix-algebra-in-r.html

# Define matrices
A <- matrix(1:100,  nrow = 10)
B <- matrix(1:1000, nrow = 10)

# Verify each is square by getting dimensions
dim(A)  # should be 10 × 10
dim(B)  # 10 × 100 — not square

# Compute inverse and determinant

# For A
invA <- solve(A)
detA <- det(A)

# View
detA

# For B, use tryCatch to capture errors
invB <- tryCatch(solve(B), error = function(e) e)
detB <- tryCatch(det(B),   error = function(e) e)

# View results
invB
detB
